<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>CPU</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container" style="margin-top: 50px">
      <div class="card">
        <div class="card-header">
          <ul class="nav nav-pills w-100">
            <li class="nav-pill active">
              <a class="nav-link">CPU </a>
            </li>
            <li class="nav-pill ml-auto">
              <a class="nav-link active" href="/CPU_CRUD/add">Add cpu</a>
            </li>
          </ul>
        </div>
        <div class="card-body">
          <% if(data.length) { %>
          <table class="table">
            <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">core</th>
                <th scope="col">GHz</th>
                <th scope="col">socket_id  (1=DDR3 2=DDR4)</th>
                
                <th scope="col">image_url</th>
                <th width="200px">Action</th>

              </tr>
            </thead>
            <tbody>
              <% for(var i = 0; i< data.length; i++) { %>
              <tr>
                <th scope="row"><%= (i+1) %></th>
                <td><%= data[i].name%></td>
                <td><%= data[i].core%></td>
                <td><%= data[i].GHz%></td>
                <td><%= data[i].socket_id%></td>
                <td><%= data[i].image_url%></td>
                
                <td>
                  <a
                    class="btn btn-success edit"
                    href="/CPU_CRUD/edit/<%=data[i].id%>"
                    >Edit</a
                  >
                  <a
                    class="btn btn-danger delete"
                    onclick="return alert('Are You sure?')"
                    href="/CPU_CRUD/delete/<%=data[i].id%>"
                    >Delete</a
                  >
                </td>
              </tr>
              <% } %>
            </tbody>
          </table>
          <% } %>

          <!-- if result is empty -->
          <% if(!data.length) { %>
          <p class="text-center">No cpu found!</p>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
